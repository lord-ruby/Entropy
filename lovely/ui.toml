[manifest]
version = "1.0.0"
dump_lua = true
priority = 1

[[patches]]
[patches.pattern]
target = "card.lua"
pattern = '''(self.ability.set == 'Voucher' and {G.C.SECONDARY_SET.Voucher, G.C.CLEAR}) or'''
position = "at"
payload = '''
(self.ability.set == 'Voucher' and {G.C.SECONDARY_SET.Voucher, G.C.CLEAR}) or (G.C.Entropy.Dissolve[self.ability.set] and {G.C.Entropy.Dissolve[self.ability.set]}) or
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "engine/moveable.lua"
pattern = '''if self.alignment.type_list.b then'''
position = "before"
payload = '''
if not self.alignment then self.alignment = {} end
if not self.alignment.offset then self.alignment.offset = {} end
if not self.alignment.offset.y then self.alignment.offset.y = 0 end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "card.lua"
pattern = "if self.ability[v] and not SMODS.Stickers[v].hide_badge then"
position = "at"
payload = '''
if self.ability[v] and not SMODS.Stickers[v].hide_badge and (v ~= "cry_rigged" or not self.ability.entr_pseudorandom) then
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "card.lua"
pattern = '''
    if not self.ability.cry_absolute then
    	if self.ability.eternal then badges[#badges + 1] = 'eternal' end
    end
'''
position = "at"
payload = '''
    if not self.ability.entr_aleph and not self.ability.cry_absolute then
    	if self.ability.eternal then badges[#badges + 1] = 'eternal' end
    end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/misc_functions.lua"
pattern = '''
  elseif args.type == 'name' then
    local temp_target = args and G.localization.descriptions[(args.set or (args.node and args.node.config and args.node.config.center and args.node.config.center.set))]
    loc_target = temp_target and temp_target[args.key or (args.node and args.node.config and args.node.config.center and args.node.config.center.key)] or "ERROR"
  end
'''
position = "at"
payload = '''
    if not self.ability.entr_aleph and not self.ability.cry_absolute then
    	if self.ability.eternal then badges[#badges + 1] = 'eternal' end
    end
'''
match_indent = true


[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = '''
                        {n=G.UIT.R,config={id = 'next_round_button', align = "cm", minw = 2.8, minh = 1.5, r=0.15,colour = G.C.RED, one_press = true, button = 'toggle_shop', hover = true,shadow = true}, nodes = {
                          {n=G.UIT.R, config={align = "cm", padding = 0.07, focus_args = {button = 'y', orientation = 'cr'}, func = 'set_button_pip'}, nodes={
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3}, nodes={
                              {n=G.UIT.T, config={text = localize('b_next_round_1'), scale = 0.4, colour = G.C.WHITE, shadow = true}}
                            }},
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3}, nodes={
                              {n=G.UIT.T, config={text = localize('b_next_round_2'), scale = 0.4, colour = G.C.WHITE, shadow = true}}
                            }}   
                          }},              
                        }},
                        {n=G.UIT.R, config={align = "cm", minw = 2.8, minh = 1.6, r=0.15,colour = G.C.GREEN, button = 'reroll_shop', func = 'can_reroll', hover = true,shadow = true}, nodes = {
                          {n=G.UIT.R, config={align = "cm", padding = 0.07, focus_args = {button = 'x', orientation = 'cr'}, func = 'set_button_pip'}, nodes={
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3}, nodes={
                              {n=G.UIT.T, config={text = localize('k_reroll'), scale = 0.4, colour = G.C.WHITE, shadow = true}},
                            }},
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3, minw = 1}, nodes={
                              {n=G.UIT.T, config={text = localize('$'), scale = 0.7, colour = G.C.WHITE, shadow = true}},
                              {n=G.UIT.T, config={ref_table = G.GAME.current_round, ref_value = 'reroll_cost', scale = 0.75, colour = G.C.WHITE, shadow = true}},
                            }}
                          }}
                        }},
'''
position = "at"
payload = '''
                        {n=G.UIT.R,config={id = 'next_round_button', align = "cm", minw = 2.8, minh = Entropy.can_switch_alt_path() and 1 or 1.5, r=0.15,colour = G.C.RED, one_press = true, button = 'toggle_shop', hover = true,shadow = true}, nodes = {
                          {n=G.UIT.R, config={align = "cm", padding = 0.07, focus_args = {button = 'y', orientation = 'cr'}, func = 'set_button_pip'}, nodes={
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3}, nodes={
                              {n=G.UIT.T, config={text = localize('b_next_round_1'), scale = 0.4, colour = G.C.WHITE, shadow = true}}
                            }},
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3}, nodes={
                              {n=G.UIT.T, config={text = localize('b_next_round_2'), scale = 0.4, colour = G.C.WHITE, shadow = true}}
                            }}   
                          }},              
                        }},
                        (G.GAME.round_resets.ante == 16 or G.GAME.round_resets.ante == 24) and {n=G.UIT.R,config={id = 'change_path_button', align = "cm", minw = 2.8, minh = 1, r=0.15,colour = G.C.RED, button = 'challenge_ee', hover = true,shadow = true, func='can_challenge_ee'}, nodes = {
                          {n=G.UIT.R, config={align = "cm", padding = 0.07, focus_args = {button = 'y', orientation = 'cr'}, func = 'set_button_pip'}, nodes={
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3}, nodes={
                              {n=G.UIT.T, config={text = localize('b_challenge_me'), scale = 0.4, colour = G.C.WHITE, shadow = true}}
                            }},
                          }},              
                        }} 
                         or (Entropy.can_switch_alt_path() and {n=G.UIT.R,config={id = 'change_path_button', align = "cm", minw = 2.8, minh = 1, r=0.15,colour = G.C.PURPLE, button = 'toggle_path', hover = true,shadow = true, func='can_toggle_path'}, nodes = {
                          {n=G.UIT.R, config={align = "cm", padding = 0.07, focus_args = {button = 'y', orientation = 'cr'}, func = 'set_button_pip'}, nodes={
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3}, nodes={
                              {n=G.UIT.T, config={text = localize(G.GAME.entr_alt and 'b_change_path_3' or 'b_change_path_1'), scale = 0.4, colour = G.C.WHITE, shadow = true}}
                            }},
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3}, nodes={
                              not G.GAME.entr_alt and {n=G.UIT.T, config={text = localize('b_change_path_2'), scale = 0.4, colour = G.C.WHITE, shadow = true}} or nil
                            }}
                          }},              
                        }} or nil),
                        {n=G.UIT.R, config={align = "cm", minw = 2.8, minh = Entropy.get_reroll_height(), r=0.15,colour = G.C.GREEN, button = 'reroll_shop', func = 'can_reroll', hover = true,shadow = true}, nodes = {
                          {n=G.UIT.R, config={align = "cm", padding = 0.07, focus_args = {button = 'x', orientation = 'cr'}, func = 'set_button_pip'}, nodes={
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3}, nodes={
                              {n=G.UIT.T, config={text = localize('k_reroll'), scale = 0.4, colour = G.C.WHITE, shadow = true}},
                            }},
                            {n=G.UIT.R, config={align = "cm", maxw = 1.3, minw = 1}, nodes={
                              {n=G.UIT.T, config={text = localize('$'), scale = 0.7, colour = G.C.WHITE, shadow = true}},
                              {n=G.UIT.T, config={ref_table = G.GAME.current_round, ref_value = 'reroll_cost', scale = 0.75, colour = G.C.WHITE, shadow = true}},
                            }}
                          }}
                        }},
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = '''
            G.STAGE == G.STAGES.MAIN_MENU and {
              label = localize('b_challenges'),
              tab_definition_function = G.UIDEF.challenges,
              tab_definition_function_args = from_game_over,
              chosen = _challenge_chosen
            } or nil,
'''
position = "at"
payload = '''
            G.STAGE == G.STAGES.MAIN_MENU and {
              label = localize('b_challenges'),
              tab_definition_function = G.UIDEF.challenges,
              tab_definition_function_args = from_game_over,
              chosen = _challenge_chosen
            } or {
              label = localize('b_daily_challenge'),
              tab_definition_function = G.UIDEF.challenge_description,
              tab_definition_function_args = "daily",
              chosen = _challenge_chosen
            },
            G.STAGE == G.STAGES.MAIN_MENU and {
              label = localize('b_daily_challenge'),
              tab_definition_function = G.UIDEF.challenge_description,
              tab_definition_function_args = "daily",
              chosen = _challenge_chosen
            } or nil,
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = '''
local challenge = G.CHALLENGES[args._id]
'''
position = "after"
payload = '''
local challenge = G.CHALLENGES[args._id] or Entropy.get_daily_challenge()
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = 'for _, v in ipairs(info_queue) do'
position = "before"
payload = '''
  if card and card.config and card.config.center then
    for i, v in pairs(Entropy.inversion_queue(card, _c, first_pass)) do
      info_queue[#info_queue + 1] = v
    end
  end
  if Entropy.deck_or_sleeve("doc") then
    if _c.key == "c_entr_beyond" then
      info_queue[#info_queue+1] = {set="Other", key="no_downside_cond"}
    end
    if _c.key == "c_cry_gateway" then
      info_queue[#info_queue+1] = {set="Other", key="no_downside"}
    end
  end
if G.GAME.entr_echo and G.GAME.entr_echo[_c.key] and first_pass then
  for i, v in pairs(G.GAME.entr_echo[_c.key]) do
    local other = G.P_CENTERS[v]
    info_queue[#info_queue+1] = {set="Other", key="echo_tooltip", vars = {G.localization.descriptions[other.set][other.key].name}}
  end
end
if card and card.ability and card.ability.debuff_timer and first_pass then
  info_queue[#info_queue+1] = {set="Other", key="temporary_debuff_tooltip", vars = {card.ability.debuff_timer}}
end
'''
match_indent = true


[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = '''
          for k, v in ipairs(row_card) do
            local card = Card(0,0, G.CARD_W*card_size, G.CARD_H*card_size, nil, G.P_CENTERS[v.id], {bypass_discovery_center = true,bypass_discovery_ui = true})
            banned_card_area:emplace(card)
          end
'''
position = "at"
payload = '''
        for k, v in ipairs(row_card) do
          if v.type == "Edition" or v.type == "Enhanced" then
              local card = Card(0,0, G.CARD_W*card_size, G.CARD_H*card_size, G.P_CARDS.empty, G.P_CENTERS[v.id], {bypass_discovery_center = true,bypass_discovery_ui = true})
              card:set_edition(v.id, true, true)
              banned_card_area:emplace(card)
          else
            local card = Card(0,0, G.CARD_W*card_size, G.CARD_H*card_size, nil, G.P_CENTERS[v.id], {bypass_discovery_center = true,bypass_discovery_ui = true})
            banned_card_area:emplace(card)
          end
        end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = "if v.eternal then card:set_eternal(true) end"
position = "before"
payload = '''
if v.stickers then
	for i, _v in ipairs(v.stickers) do
		SMODS.Stickers[_v]:apply(card, true)
	end
end
'''
match_indent = true

# same deal for actual game
[[patches]]
[patches.pattern]
target = "game.lua"
pattern = "if v.eternal then _joker:set_eternal(true) end"
position = "before"
payload = '''
if v.stickers then
	for i, _v in ipairs(v.stickers) do
		SMODS.Stickers[_v]:apply(_joker, true)
	end
end
'''
match_indent = true

# remove eternal badge
[[patches]]
[patches.pattern]
target = "card.lua"
pattern = "if self.ability.eternal then badges[#badges + 1] = 'eternal' end"
position = "at"
payload = '''
if not self.ability.entr_aleph then
	if self.ability.eternal then badges[#badges + 1] = 'eternal' end
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = '''functions/common_events.lua'''
pattern = '''if main_end then'''
position = "before"
payload = '''
if cfg and cfg.cry_multiuse then
	local loc = {}
	localize{type = 'other', key = 'cry_multiuse', nodes = loc, vars = {cfg.cry_multiuse, colours = {get_type_colour(cfg, card)}}}
	desc_nodes[#desc_nodes+1] = loc[1]
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = '''functions/common_events.lua'''
pattern = '''if cfg and cfg.cry_multiuse then
	local loc = {}
	localize{type = 'other', key = 'cry_multiuse', nodes = loc, vars = {cfg.cry_multiuse}}
	desc_nodes[#desc_nodes+1] = loc[1]
end'''
position = "at"
payload = '''

'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = '''if (prev_state == G.STATES.TAROT_PACK or prev_state == G.STATES.PLANET_PACK or'''
position = "before"
payload = '''
if cry_muse and not (SMODS.Mods["Cryptid"] or {}).can_load then
	card.ability.cry_multiuse = card.ability.cry_multiuse - 1
	card.ability.extra_value = -1 * math.max(1, math.floor(card.cost/2))
	card:set_cost()
	delay(0.4)

	-- i make my own card eval status text :D

	card:juice_up()
	play_sound('generic1')
	attention_text({
		text = format_ui_value(card.ability.cry_multiuse),
		scale = 1.1,
		hold = 0.6,
		major = card,
		backdrop_colour = G.C.SET[card.config.center.set],
		align = 'bm',
		offset = {x = 0, y = 0.2}
	})

	delay(0.8)
 	if card.playing_card then
		draw_card(G.play, G.hand, 1, 'up', true, card, nil, true)
 	elseif not G.GAME.modifiers.cry_beta then
		if to_big(#G.consumeables.cards) < to_big(G.consumeables.config.card_limit) then
			draw_card(G.play, G.consumeables, 1, 'up', true, card, nil, true)
		else card:start_dissolve()
		end
  	else
		if to_big(#G.jokers.cards) < to_big(G.jokers.config.card_limit) then
			draw_card(G.play, G.jokers, 1, 'up', true, card, nil, true)
		else card:start_dissolve()
		end
    end
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = '''if card.area and (not nc or card.area == G.pack_cards) then card.area:remove_card(card) end'''
position = "before"
payload = '''
local cry_muse = false
if card.ability.cry_multiuse and not (card.ability.cry_multiuse <= 1) then
	cry_muse = true
	dont_dissolve = true
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = '''if AUT.card_type == 'Joker' or (AUT.badges and AUT.badges.force_rarity) then card_type = SMODS.Rarity:get_rarity_badge(card.config.center.rarity) end'''
position = "at"
payload = '''
if AUT.card_type == 'Joker' or (AUT.badges and AUT.badges.force_rarity) then 
  local rarity = card.ability.glitched_crown and G.P_CENTERS[card.ability.glitched_crown[card.glitched_index]] and G.P_CENTERS[card.ability.glitched_crown[card.glitched_index]].rarity or card.config.center.rarity
  card_type = SMODS.Rarity:get_rarity_badge(rarity) 
end
'''
match_indent = true 

[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = '''
local banned_cards, banned_tags, banned_other = {}, {}, {}
'''
position = "after"
payload = '''
local banned_cards, banned_tags, banned_other = {}, {}, {}
    if challenge.restrictions and challenge.restrictions.banned_cards then
      local banned = {}
      for i, v in pairs(challenge.restrictions.banned_cards) do
          if G.P_CENTERS[v.id] then
            banned[#banned+1] = v
          end
      end
      challenge.restrictions.banned_cards = banned
    end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = '''=[SMODS _ "src/game_object.lua"]'''
pattern = '''
if center.set ~= self.key then SMODS.insert_pool(G.P_CENTER_POOLS[self.key], center) end
'''
position = "before"
payload = '''
if not center then return end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = '''game.lua'''
pattern = '''
if _ch.consumeables then
    for k, v in ipairs(_ch.consumeables) do
        G.E_MANAGER:add_event(Event({
            func = function()
                add_joker(v.id, nil, k ~= 1)
            return true
            end
        }))
    end
end
'''
position = "at"
payload = '''
if _ch.consumeables then
    for k, v in ipairs(_ch.consumeables) do
        G.E_MANAGER:add_event(Event({
            func = function()
                local _joker = add_joker(v.id, v.edition, k ~= 1)
                if v.stickers then
                  for i, _v in ipairs(v.stickers) do
                    SMODS.Stickers[_v]:apply(_joker, true)
                  end
                end
                if v.eternal then _joker:set_eternal(true) end
                if v.pinned then _joker.pinned = true end
                if v.rental then _joker:set_rental(true) end
            return true
            end
        }))
    end
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = '''functions/common_events.lua'''
pattern = '''
config.saved and 
{n=G.UIT.C, config={padding = 0.05, align = 'cm'}, nodes={
'''
position = "at"
payload = '''
config.saved and G.GAME.saved_text and not G.GAME.no_saved and
{n=G.UIT.C, config={padding = 0.05, align = 'cm'}, nodes={
'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/UI_definitions.lua'
pattern = '''
  local t =   create_UIBox_generic_options({padding = 0,contents ={
      {n=G.UIT.R, config={align = "cm", padding = 0, draw_layer = 1, minw = 4}, nodes={
        create_tabs(
        {tabs = {
            {
                label = 1,
                chosen = G.focused_profile == 1,
                tab_definition_function = G.UIDEF.profile_option,
                tab_definition_function_args = 1
            },
            {
                label = 2,
                chosen = G.focused_profile == 2,
                tab_definition_function = G.UIDEF.profile_option,
                tab_definition_function_args = 2
            },
            {
                label = 3,
                chosen = G.focused_profile == 3,
                tab_definition_function = G.UIDEF.profile_option,
                tab_definition_function_args = 3
            }
        },
'''
position = "at"
payload = '''
if not Cryptid then Cryptid = {profile_prefix = Entropy.config.profile_prefix and "E" or ""} end;
  local t =   create_UIBox_generic_options({padding = 0,contents ={
      {n=G.UIT.R, config={align = "cm", padding = 0, draw_layer = 1, minw = 4}, nodes={
        create_tabs(
        {tabs = {
             {
                label = Cryptid.profile_prefix..1,
                chosen = G.focused_profile == Cryptid.profile_prefix..1,
                tab_definition_function = G.UIDEF.profile_option,
                tab_definition_function_args = Cryptid.profile_prefix..1
            },
            {
                label = Cryptid.profile_prefix..2,
                chosen = G.focused_profile == Cryptid.profile_prefix..2,
                tab_definition_function = G.UIDEF.profile_option,
                tab_definition_function_args = Cryptid.profile_prefix..2
            },
            {
                label = Cryptid.profile_prefix..3,
                chosen = G.focused_profile == Cryptid.profile_prefix..3,
                tab_definition_function = G.UIDEF.profile_option,
                tab_definition_function_args = Cryptid.profile_prefix..3
            }
        },
'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/UI_definitions.lua'
pattern = '''
{n=G.UIT.O, config={object = DynaText({string = {{prefix = localize('$'), ref_table = card, ref_value = 'cost'}}, colours = {G.C.MONEY},shadow = true, silent = true, bump = true, pop_in = 0, scale = 0.5})}},
'''
position = 'at'
payload = '''
{n=G.UIT.O, config={object = DynaText({string = {{prefix = not card.ability.beast_mark and localize('$') or nil, ref_table = not card.ability.beast_mark and card or {cost = (_G.type(card.ability.beast_mark) == "number" and number_format(card.ability.beast_mark) or "1").." Card"..(_G.type(card.ability.beast_mark) == "number" and card.ability.beast_mark > 1 and "s" or "")}, ref_value = 'cost'}}, colours = {card.ability.beast_mark and G.C.RED or G.C.MONEY},shadow = true, silent = true, bump = true,pop_in = 0, scale = 0.5})}},
'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'tag.lua'
pattern = '''
self.HUD_tag.states.visible = false
'''
position = 'at'
payload = '''
if self.HUD_tag then self.HUD_tag.states.visible = false end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'tag.lua'
pattern = '''
self.HUD_tag:remove()
'''
position = 'at'
payload = '''
if self.HUD_tag then self.HUD_tag:remove() end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = '''=[SMODS _ "src/utils.lua"]'''
pattern = '''
G.HUD:get_UIE_by_ID('hand_operator_container').UIBox:recalculate()
'''
position = 'at'
payload = '''
if G.HUD:get_UIE_by_ID('hand_operator_container') and G.HUD:get_UIE_by_ID('hand_operator_container').UIBox then
  G.HUD:get_UIE_by_ID('hand_operator_container').UIBox:recalculate()
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = '''=[SMODS _ "src/utils.lua"]'''
pattern = '''
{n=G.UIT.R, config={align = "cm", colour = mod.badge_colour or G.C.GREEN, r = 0.1, minw = 2, minh = 0.36, emboss = 0.05, padding = 0.03*size}, nodes={
'''
position = 'at'
payload = '''
{n=G.UIT.R, config={align = "cm", colour = mod.badge_colour or G.C.GREEN, shader = shader, r = 0.1, minw = 2, minh = 0.36, emboss = 0.05, padding = 0.03*size}, nodes={
'''
match_indent = true

[[patches]]
[patches.pattern]
target = '''=[SMODS _ "src/utils.lua"]'''
pattern = '''
for i, mod in ipairs(mods) do
  local mod_name = mod.display_name
'''
position = 'after'
payload = '''
local shader
if mod.id == "entr" then shader = "entr_brimstone_badge" end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/UI_definitions.lua'
pattern = '''
elseif not run_info then
'''
position = 'before'
payload = '''
elseif type == 'Red' then
  local dt1 = DynaText({string = {{string = localize('ph_red_1'), colour = G.C.RED}}, colours = {G.C.BLACK}, scale = 0.55, silent = true, pop_delay = 4.5, shadow = true, bump = true, maxw = 3})
    local dt2 = DynaText({string = {{string = localize('ph_red_2'), colour = G.C.WHITE}},colours = {G.C.CHANCE}, scale = 0.35, silent = true, pop_delay = 4.5, shadow = true, maxw = 3})
    extras =
    {n=G.UIT.R, config={align = "cm"}, nodes={
      {n=G.UIT.R, config={align = "cm", padding = 0.07, r = 0.1, colour = {0,0,0,0.12}, minw = 2.9}, nodes={
        {n=G.UIT.R, config={align = "cm"}, nodes={
          {n=G.UIT.O, config={object = dt1}},
        }},
        {n=G.UIT.R, config={align = "cm"}, nodes={
          {n=G.UIT.O, config={object = dt2}},
        }},
      }},
    }}
'''
match_indent = true

[[patches]]
[patches.pattern]
target = '''tag.lua'''
pattern = '''
_self.config.h_popup_config = (_self.T.x > G.ROOM.T.w*0.4) and
  {align =  'cl', offset = {x=-0.1,y=0},parent = _self} or
  {align =  'cr', offset = {x=0.1,y=0},parent = _self}
'''
position = 'after'
payload = '''
if G.P_RUNES[self.key] then
_self.config.h_popup_config = (_self.T.y > G.ROOM.T.y*0.2) and
  {align =  'cl', offset = {x=-0.1,y=0},parent = _self} or
  {align =  'cl', offset = {x=-0.1,y=0.65},parent = _self}
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/UI_definitions.lua'
match_indent = true
position = 'after'
pattern = '''
info_boxes = {{n=G.UIT.R, config = {align="cm", padding = 0.05, card_pos = card.T.x }, nodes = info_cols}}
'''
payload = '''
local AUT_ENTR_EPITACHYNO
if card.ability and card.ability.epitach_consumeable then
    AUT_ENTR_EPITACHYNO = Card.generate_UIBox_ability_table(Entropy.get_dummy(G.P_CENTERS[card.ability.epitach_consumeable], G.consumeables, card))
end
'''

[[patches]]
[patches.pattern]
target = 'functions/UI_definitions.lua'
match_indent = true
position = 'before'
pattern = '''
if multi_boxes[1] then
'''
payload = '''
if AUT_ENTR_EPITACHYNO then
local target = ret_val.nodes[1].nodes[1].nodes[1].nodes
    table.insert(target, #target, name_from_rows(AUT_ENTR_EPITACHYNO.name, is_playing_card and G.C.WHITE or nil))
    table.insert(target, #target, desc_from_rows(AUT_ENTR_EPITACHYNO.main))
end
'''

[[patches]]
[patches.pattern]
target = '''functions/UI_definitions.lua'''
match_indent = true
position = 'at'
pattern = '''
{n=G.UIT.R, config={align = "cm"}, nodes={
  {n=G.UIT.C, config={align = "cm", minw = lwidth, maxw = lwidth}, nodes={{n=G.UIT.T, config={text = localize('k_round'),colour = G.C.UI.TEXT_DARK, scale = scale*0.8}}}},
  {n=G.UIT.C, config={align = "cm"}, nodes={{n=G.UIT.T, config={text = ': ',colour = G.C.UI.TEXT_DARK, scale = scale*0.8}}}},
  {n=G.UIT.C, config={align = "cl", minw = rwidth, maxw = lwidth}, nodes={{n=G.UIT.T, config={text = tostring(saved_game.GAME.round),colour = G.C.RED, scale = 0.8*scale}}}}
}},
{n=G.UIT.R, config={align = "cm"}, nodes={
  {n=G.UIT.C, config={align = "cm", minw = lwidth, maxw = lwidth}, nodes={{n=G.UIT.T, config={text = localize('k_ante'),colour = G.C.UI.TEXT_DARK, scale = scale*0.8}}}},
  {n=G.UIT.C, config={align = "cm"}, nodes={{n=G.UIT.T, config={text = ': ',colour = G.C.UI.TEXT_DARK, scale = scale*0.8}}}},
  {n=G.UIT.C, config={align = "cl", minw = rwidth, maxw = lwidth}, nodes={{n=G.UIT.T, config={text = tostring(saved_game.GAME.round_resets.ante),colour = G.C.BLUE, scale = 0.8*scale}}}}
}},
'''
payload = '''
{n=G.UIT.R, config={align = "cm"}, nodes={
  {n=G.UIT.C, config={align = "cm", minw = lwidth, maxw = lwidth}, nodes={{n=G.UIT.T, config={text = localize('k_round'),colour = G.C.UI.TEXT_DARK, scale = scale*0.8}}}},
  {n=G.UIT.C, config={align = "cm"}, nodes={{n=G.UIT.T, config={text = ': ',colour = G.C.UI.TEXT_DARK, scale = scale*0.8}}}},
  {n=G.UIT.C, config={align = "cl", minw = rwidth, maxw = lwidth}, nodes={{n=G.UIT.T, config={text = tostring(saved_game.GAME.round),colour = G.SAVED_GAME.GAME.EEBuildup and Entropy.reverse_legendary_gradient or G.C.RED, scale = 0.8*scale}}}}
}},
{n=G.UIT.R, config={align = "cm"}, nodes={
  {n=G.UIT.C, config={align = "cm", minw = lwidth, maxw = lwidth}, nodes={{n=G.UIT.T, config={text = localize('k_ante'),colour = G.C.UI.TEXT_DARK, scale = scale*0.8}}}},
  {n=G.UIT.C, config={align = "cm"}, nodes={{n=G.UIT.T, config={text = ': ',colour = G.C.UI.TEXT_DARK, scale = scale*0.8}}}},
  {n=G.UIT.C, config={align = "cl", minw = rwidth, maxw = lwidth}, nodes={{n=G.UIT.T, config={text = tostring(saved_game.GAME.round_resets.ante),colour = G.SAVED_GAME.GAME.EEBuildup and Entropy.reverse_legendary_gradient or G.C.BLUE, scale = 0.8*scale}}}}
}},
'''

[[patches]]
[patches.pattern]
target = '''functions/button_callbacks.lua'''
pattern = '''
G.blind_prompt_box.alignment.offset.y = -10
G.blind_select.alignment.offset.y = 40
G.blind_select.alignment.offset.x = 0
'''
position = "at"
payload = '''
if G.blind_prompt_box then
  G.blind_prompt_box.alignment.offset.y = -10
end
if G.blind_select then
  G.blind_select.alignment.offset.y = 40
  G.blind_select.alignment.offset.x = 0
end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = '''functions/button_callbacks.lua'''
pattern = '''
G.blind_select:remove()
G.blind_prompt_box:remove()
G.blind_select = nil
'''
position = "at"
payload = '''
if G.blind_prompt_box then
  G.blind_prompt_box:remove()
end
if G.blind_select then
  G.blind_select:remove()
  G.blind_select = nil
end
'''
match_indent = true